# Local development: training, EDA, notebooks, database loading and tests.
# Installs the Streamlit runtime set on top.
-r requirements.txt

# Database
python-dotenv==1.0.1
sqlalchemy==2.0.27
psycopg2-binary==2.9.9

# Notebooks and EDA
jupyter==1.0.0
notebook==7.1.1
# 3.9+ dropped the numpy<2 cap; 3.8.3 conflicts with the pinned numpy==2.2.3 and broke CI.
matplotlib==3.10.7
seaborn==0.13.2

# API (served in production from requirements-api.txt / Dockerfile)
fastapi==0.115.6
uvicorn==0.32.1

# Tooling
pytest==8.3.4
httpx==0.28.1
