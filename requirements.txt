# Streamlit Cloud runtime only. Keep this list minimal — every extra package lengthens
# the Cloud build and risks a source build when no wheel exists for the runner's Python.
# Training/notebook/API tooling lives in requirements-dev.txt and requirements-api.txt.
streamlit==1.40.2
pandas==2.2.3
numpy==2.2.3
# Must match the version the .pkl bundles were fitted with, or unpickling warns/fails.
scikit-learn==1.8.0
joblib==1.5.3
huggingface_hub>=0.23.0
